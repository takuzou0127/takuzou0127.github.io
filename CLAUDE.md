# このリポジトリで作業するClaude Code（クラウド）へ

このセッションは、過去のセッションの会話を覚えていない。過去を知る手段は、このリポジトリの **main にあるファイルだけ**。

## セッション開始時に必ず読む
1. `context.json` を**全文**読む（一部だけ読んで答えない）
2. `cloud-memory.md` を全文読む。PCの日次更新で `context.json` から消えた決定・訂正が入っている。`context.json` と食い違ったら日付の新しい方に従う

## 決定・訂正を記録する時
- ユーザーとの会話で決まったこと（保有の変更、ルール、訂正）は **`cloud-memory.md` に日付付きで追記**する
- `context.json` だけに書かない。PCの日次更新（"Daily update" コミット）が `context.json` を丸ごと上書きするため、クラウドで書いた追記は消える（実例：9/24の「NISAのコスト訂正」が9/25以降消えた）
- 記録したら、**その会話の中で main まで取り込む**。ブランチのままだと次のセッションから見えない（実例：9/24の「ADR→000660に乗り換えた理由」がブランチに残ったまま埋もれた）
